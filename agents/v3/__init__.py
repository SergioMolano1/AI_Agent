# v3 Agent Package - Hybrid Architecture (Deterministic Detection + LLM Report)
# Note: create_orchestrator and run_agent require google-adk installed.
# Import them explicitly when needed:
#   from agents.v3.agent import create_orchestrator, run_agent
